---
"@openproject/primer-view-components": minor
---

`DataTable`: `Column` accepts `cell_classes:` and `cell_data:` procs, called with each row, so consumers can vary a cell's CSS classes and data attributes per row without a custom cell renderer
